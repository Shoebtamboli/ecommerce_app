<!DOCTYPE html>
<html>
  <head>
    <title>EcommerceApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      .min-h-screen {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }
      .flex-grow {
        flex-grow: 1;
      }
    </style>
  </head>
  <body class="flex flex-col min-h-screen">
    <%= render 'layouts/navbar' %>
    <main class="flex-grow container mx-auto mt-8 px-5">
      <% if notice %>
        <p class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert"><%= alert %></p>
      <% end %>
      <%= yield %>
    </main>
    <%= render 'layouts/footer' %>
  </body>
</html>
